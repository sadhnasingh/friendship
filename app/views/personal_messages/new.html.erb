<h1>New message to <%= @friend.name %></h1>

<%= form_for @personal_message do |f| %>
  <%= hidden_field_tag 'friend_id', @friend.id %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit %>
<% end %>